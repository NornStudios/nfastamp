# nfastamp.fun

Vite + React marketing site for **NFA** (Not Financial Advice).

- Landing: `/`
- Arcade: `/arcade` (drip-unlock games, DEMO CHIPS, transparent 5% house edge)

```bash
npm i && npm run build
```

Config: `src/config.ts`. Arcade drip: `src/arcade/dripSchedule.ts`.
